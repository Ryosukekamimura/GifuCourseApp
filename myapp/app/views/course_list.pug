extends layout

block content
    h1= title

    ul
        each course in course_list
            li
                a(href=course.url) #{course.lecture_name}
                | (#{course.lecture_season}
        else
            li There are no courses